apiVersion: v1
kind: Service
metadata:
  name: {{ include "shimin-for-fun.fullname" . }}
  labels:
    {{- include "shimin-for-fun.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shimin-for-fun.selectorLabels" . | nindent 4 }}
